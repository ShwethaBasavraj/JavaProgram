public class Cashier {
    public static void collectPayment(String customerName, String movieName, double amount) {
        System.out.println("Collecting payment of " + amount + " from " + customerName + " for " + movieName);
    }
}
